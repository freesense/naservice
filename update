cd aria2
docker-compose pull
docker-compose down
docker-compose up -d

cd ../jellyfin
docker-compose pull
docker-compose down
docker-compose up -d

cd ../nextcloud
docker-compose pull
docker-compose down
docker-compose up -d

cd ../mydb
docker-compose pull
docker-compose down
docker-compose up -d

cd ../portainer
docker-compose pull
docker-compose down
docker-compose up -d

cd ../qbittorrent
docker-compose pull
docker-compose down
docker-compose up -d

cd ../siyuan
docker-compose pull
docker-compose down
docker-compose up -d

cd ../trilium
docker-compose pull
docker-compose down
docker-compose up -d

cd ../alist
docker-compose pull
docker-compose down
docker-compose up -d

cd ../calibre
docker-compose pull
docker-compose down
docker-compose up -d

docker image ls

